{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}

    <div class="container" style="min-height: 600px;margin:5% 20%">
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
            
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ voiture.modele }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ voiture.annee ? voiture.annee|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ voiture.type }}</td>
            </tr>
            <tr>
                <th>Desponibilite</th>
                <td>{{ voiture.desponibilite ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn" href="{{ path('app_voiture_index') }}">back to list</a>

    <a class="btn" href="{{ path('app_voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voiture/_delete_form.html.twig') }}
    </div>
{% endblock %}
